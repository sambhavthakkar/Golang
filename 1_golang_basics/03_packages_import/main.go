package main

//imports briings external packages into the fine that u are working
// //where actuallu u need it
import(
	"fmt"
	"math"
)

func main(){
//packageName.FunctionName -> call func from package

fmt.Println("sqrt(25):", math.Sqrt(25))
	
}